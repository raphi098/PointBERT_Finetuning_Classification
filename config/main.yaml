defaults:
  - _self_
  - model: pointbert   

dataset:
  num_classes: 14   
  npoints: 8192
  batch_size: 8
  data_path: dataset/pretrain_pointbert_8192 #Important
  random_rotation: True

mlflow_experiment_name: Classification_Backbone_Comparrison

mode: train
test_path: ./experiments/bruecke/baseline
epochs: 300

hydra:
  run:
    dir: ./experiments/bruecke/20_epochs_1024_best
  job:
    chdir: True